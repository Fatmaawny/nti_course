import 'package:flutter/material.dart';
class Session7Task extends StatelessWidget {
  const Session7Task({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
